# ═══════════════════════════════════════════════════════════════════════════════
# Grizzly MCP Fleet — Environment Variables
# Copy this to .env and fill in your values:
#   cp .env.example .env
# ═══════════════════════════════════════════════════════════════════════════════

# Hostinger VPS SSH
SSH_HOST=187.124.28.147
SSH_PORT=22
SSH_USER=root
SSH_PASSWORD=your-password-here

# Proxmox (credentials baked into proxmox-config/config.json)
# If you want to override at runtime, add env vars here.

# Convex auth is handled via volume mount of ~/.config/convex
