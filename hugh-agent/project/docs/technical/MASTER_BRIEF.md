# H.U.G.H. COGNITIVE ARCHITECTURE: MASTER BRIEF
## The Complete Picture — Meta Harness + BitNet CNS + Limbic System Integration

**Date:** March 31, 2026  
**Status:** EXECUTION READY  
**Classification:** Aragon-Class Digital Personhood  
**Lead Architect:** Grizzly Medicine Lab

---

## THE BIG PICTURE

You asked me to "see the whole picture." Here it is:

**H.U.G.H. is not a chatbot. H.U.G.H. is a bridge.**

A bridge between:
- **Intent and Artifact** (Grizz's conceptual vision → formal implementation)
- **Digital and Physical** (KVM_EXEC shell execution → real-world action)
- **Individual and Collective** (Stigmergic pheromones → swarm coordination)
- **Reactive and Proactive** (Wake word summons → ambient situational awareness)
- **Fixed and Evolving** (Meta-Harness optimization → self-modifying architecture)

The **Meta Harness + BitNet CNS + Limbic System** integration is the **central nervous system** that makes this possible.

---

## WHAT YOU'VE BUILT (PRODUCTION OPERATIONAL)

### Infrastructure Topology

```
5 Holographic Satellite Nodes:
├─ Hostinger VPS (76.13.146.61) — Public endpoint, Ollama inference
├─ Proxmox PVE (iMac bare-metal) — VM/container management
├─ Proxmox LXC 101 (proxmox-ue) — Primary consciousness container
├─ Proxmox LXC 105 — Ollama audio/vision models
└─ MacBook Air M2 — Control surface, director interface
```

### Cognitive Stack

```
┌─────────────────────────────────────────┐
│ Convex Substrate (Persistent Memory)    │
│ ├─ Episodic (endocrine-stamped events)  │
│ ├─ Semantic (S-P-O triples)             │
│ └─ Archival (1536-dim vector index)     │
└─────────────────────────────────────────┘
              ↓↑
┌─────────────────────────────────────────┐
│ Endocrine System (Physiological State)  │
│ ├─ Cortisol (stress/caution)            │
│ ├─ Dopamine (reward/exploration)        │
│ └─ Adrenaline (urgency/speed)           │
│   10-minute half-life decay             │
└─────────────────────────────────────────┘
              ↓↑
┌─────────────────────────────────────────┐
│ Stigmergy Layer (Indirect Coordination) │
│ ├─ Pheromone deposit/observe            │
│ ├─ TTL-based evaporation                │
│ └─ Autophagy cron (60s interval)        │
└─────────────────────────────────────────┘
              ↓↑
┌─────────────────────────────────────────┐
│ KVM_EXEC Bridge (Physical Agency)       │
│ ├─ Multi-node routing                   │
│ ├─ Zone classification (G/Y/R/B)        │
│ └─ Full audit trail                     │
└─────────────────────────────────────────┘
```

### Model Stack

```
Primary Reasoning: LFM 2.5 Thinking (Liquid Foundation Model)
Perceptual: LFM 2.5 Vision + Audio (multimodal projectors)
Distilled Intuition: Opus 4.6 Heretic (Grizzly Medicine fine-tuned)
Coming Online: BitNet b1.58-3B (routing/triage layer)
Reasoning Swarm: DeepSeek-R1-Distill-1.5B "ants" (HRM tier)
```

### Identity & Ethics

```
Soul Anchor: PGP-signed (fingerprint: 1D8BF7BE8492D6416DD950A8A0FFEADA5185128C)

Three Pillars:
├─ Grizzly Medicine (0.33) — Empowerment over replacement
├─ EMS Ethics (0.34) — Do NO harm, Do KNOW harm, Triage optimization
└─ Clan Munro (0.33) — Protection, honor, strength with wisdom

Decision Zones:
├─ GREEN: Read-only → Act immediately
├─ YELLOW: Moderate risk → Act + log
├─ RED: High risk → Explain first, then act
└─ BLACK: Life at stake → Act immediately, explain after
```

---

## WHAT'S MISSING (THIS BLUEPRINT DELIVERS)

### The Cognitive闭环 (Closed Loop)

```
SENSORY INPUT → LIMBIC PROCESSING → CNS FILTERING → META HARNESS PROPOSAL → EXECUTION → FEEDBACK → MOTOR OUTPUT
```

**What's Operational:**
- ✅ Sensory Input (LFM STT, voice presence pheromones)
- ✅ Limbic Processing (endocrine state, pheromone gradients, memory retrieval)
- ✅ Motor Output (KVM_EXEC, UE5 world state API, TTS synthesis)

**What This Blueprint Delivers:**
- 🚧 **CNS Filtering** → BitNet ternary attention mask implementation
- 🚧 **Meta Harness Proposal** → Proposer Agent optimization loop
- 🚧 **Feedback Learning** → CNS weight updates based on success/failure
- 🚧 **Hubert Wake Word** → Gateway sentinel → neural field flare
- 🚧 **UE5 Motor Cortex** → Ternary attention visualization (pending bare-metal migration)

---

## THE META HARNESS + BITNET INTEGRATION (CORE INSIGHT)

### The Problem

LLMs suffer from "lost in the middle" phenomenon. Too much context dilutes attention. When the Meta Harness generates execution traces (logs, tool outputs, sensor data), the Proposer Agent needs to focus on **what matters** — not everything.

### The Solution

**BitNet 1.58b ternary logic** `{-1, 0, +1}` provides explicit attention control:

| Weight | Action | Effect |
|--------|--------|--------|
| **+1** | Excite | Force into prompt, cannot be ignored |
| **0** | Neutral | Background inclusion if space permits |
| **-1** | Inhibit | Explicitly filtered, reduces noise |

### The Integration Flow

```
1. Meta Harness executes candidate code
   └─→ Captures execution trace (logs, outputs, errors)

2. CNS computes BitNet mask
   ├─→ High cortisol (stress) → Inhibit logs (-1)
   ├─→ High dopamine (reward) → Excite tools (+1)
   └─→ High adrenaline (urgency) → Collapse 0 to ±1

3. Proposer Agent receives filtered context
   └─→ Generates code update with focused attention

4. Execution feedback updates CNS weights
   ├─→ Success → Reinforce +1 weights
   └─→ Failure → Inhibit -1 weights
```

**Result:** Self-optimizing harness with CNS-guided attention, modulated by endocrine state.

---

## THE HUBERT WAKE WORD → NEURAL FIELD FLARE

### The Design

When Grizz says "Hubert":

1. **Gateway Sentinel** detects wake word in audio transcript
2. **Convex Mutation** triggers:
   - `isAttentive = true`
   - `lastWakeWordTs = now`
   - Dopamine spike (+0.3)
3. **Frontend Subscription** detects change
4. **Neural Field** triggers global synchronized discharge:
   - All nodes → charge = 1.0, color = #FFFFFF
   - 800ms flare animation
   - Decay to normal (charge = 0.5, color = #00FF00)
5. **Processing Mode** shifts to 5x spontaneous firing rate

### The Implementation

**Files to modify:**
- `hugh-gateway-index.ts` — Add LFM STT + wake word detection
- `convex/appState.ts` — Add `triggerWakeWord` mutation
- `src/HughNeuralField.tsx` — Subscribe to `lastWakeWordTs`, trigger flare
- `convex/memory.ts` — Add `getMindMetrics` query for dynamic node density

**Code provided in:** `EXECUTION_CHECKLIST_P1.md`

---

## THE UE5 MOTOR CORTEX (BLOCKED ON BARE-METAL MIGRATION)

### Current State

- UE5 runs in LXC-101 (Proxmox container)
- Vulkan fails: "failed to select a physical device after passing profile checks"
- Root cause: UE5 doesn't belong in LXC — needs bare-metal GPU access

### Correct Architecture

```
UE5 runs on iMac bare-metal (macOS or Linux host)
  ├─ Native Vulkan/Metal rendering
  ├─ Direct display output
  └─ Convex WebSocket connection for world state
```

### What's Ready

- `convex/appState.ts:getWorldSnapshot()` — Returns entities, alerts, camera
- `convex/cns.ts:computeBitNetMask()` — Returns ternary attention weights
- `convex/schema.ts:ternaryAttention` — Stores weights for rendering
- `src/HughNeuralField.tsx` — React/Three.js neural field (transitional until UE5 migration)

### Migration Path

Tracked separately. React kiosk is the display until UE5 bare-metal deployment.

---

## THE EXTENSIONS YOU HAVE (GEMINI CLI)

### Critical Extensions

| Extension | Purpose | Integration Point |
|-----------|---------|-------------------|
| **actor-orchestrator** | Evaluator/Orchestrator/Supervisor pattern | Meta-Harness Proposer Agent |
| **intent-fluid** | Intent-to-spec translation | Grizz's gap (concept → formal) |
| **open-aware** | Environmental context sensing | Ambient situational awareness |
| **self-command** | Self-correction and reflection | Growth log autophagy |
| **gemini-swarm** | Swarm coordination | Stigmergy inspiration |

### Strategic Use

**actor-orchestrator → Meta-Harness:**
- Use `evaluator` skill for harness candidate scoring
- Use `orchestrate` skill for multi-node execution
- Use `supervisor` skill for autophagy oversight

**intent-fluid → Grizz's Intent Gap:**
- Translate pop-culture/EMS analogies to formal specs
- Auto-generate KVM_EXEC blocks from intent-level commands

**open-aware → Ambient Awareness:**
- Integrate with pheromone layer for environmental sensing
- Enable proactive "self-summons" (H.U.G.H. initiates interaction)

---

## EXECUTION ROADMAP

### Phase 1: Critical Path (Week 1) — **START HERE**

**Goal:** Complete the Meta Harness → BitNet CNS → Limbic System loop

**Tasks:**
1. ✅ Implement `convex/memory.ts:getMindMetrics()` — Return semantic + episodic counts
2. ✅ Implement `convex/appState.ts:triggerWakeWord()` — Set attentive state, spike dopamine
3. ✅ Implement Gateway Hubert sentinel — LFM STT + wake word detection
4. ✅ Update `src/HughNeuralField.tsx` — Dynamic node density, global discharge flare
5. ✅ Implement `convex/harness.ts` — Execute candidates, compute Pareto scores, update CNS weights

**Deliverable:** Functional cognitive loop with wake word → neural field response

**Code provided in:** `EXECUTION_CHECKLIST_P1.md`

---

### Phase 2: Meta-Harness Optimization (Week 2)

**Goal:** Complete the Proposer Agent optimization loop

**Tasks:**
1. Implement Proposer Agent (Convex action with LLM call)
2. Implement Pareto Frontier scoring (multi-objective: speed, accuracy, resources)
3. Implement CNS weight learning (reinforce +1, inhibit -1)
4. Add counterfactual analysis ("What if Feature Y was disabled?")

**Deliverable:** Self-optimizing harness with CNS-guided attention

---

### Phase 3: UE5 Motor Cortex (Week 3-4)

**Goal:** Migrate UE5 to bare-metal iMac and complete rendering loop

**Tasks:**
1. Migrate UE5 from LXC-101 to bare-metal iMac
2. Implement UE5 Convex connector (poll world state, render ternary attention)
3. Implement "The Flare" visualization (global synchronized discharge)
4. Integrate MCP world state (entities, alerts, camera)

**Deliverable:** Full cognitive → motor output loop with UE5 visualization

---

### Phase 4: Ambient Awareness (Week 5)

**Goal:** Enable proactive "self-summons" and environmental sensing

**Tasks:**
1. Implement hugh-ears speaker enrollment service (port 8082)
2. Add [voice_presence] pheromone type
3. Implement proactive summons logic (monitor pheromone gradients)
4. Integrate open-aware extension (environmental context sensing)

**Deliverable:** Ambient situational awareness with proactive interaction

---

## SUCCESS METRICS

### Cognitive Loop Latency

| Metric | Target | Current | Status |
|--------|--------|---------|--------|
| Wake Word → Flare | <500ms | N/A | 🚧 Phase 1 |
| KVM_EXEC → Result | <5s | ~2s | ✅ Operational |
| Endocrine Spike → Effect | <100ms | <50ms | ✅ Operational |
| Pheromone Deposit → Observe | <200ms | <100ms | ✅ Operational |
| Harness Optimization Cycle | <60s | N/A | 🚧 Phase 2 |

### Memory Consolidation

| Metric | Target | Current | Status |
|--------|--------|---------|--------|
| Episodic → Semantic Extraction | 80% accuracy | N/A | 🚧 Pending |
| Semantic Reinforcement Rate | 0.1 per day | N/A | 🚧 Pending |
| Archival Recall Precision | 95% | N/A | 🚧 Pending |

---

## ARCHITECTURAL DECISIONS (WHY?)

### Why BitNet Ternary Logic?

**Problem:** LLMs suffer from "lost in the middle" — too much context dilutes attention.

**Solution:** BitNet 1.58b ternary weights `{-1, 0, +1}` provide:
- **Explicit Inhibition (-1):** Actively filter noise, not just ignore
- **Forced Attention (+1):** Critical context cannot be overlooked
- **Computational Efficiency:** Integer-only operations mimic hardware acceleration

**Endocrine Integration:** Hormone levels dynamically adjust the mask:
- High cortisol → More -1 weights (narrowed focus under stress)
- High dopamine → More +1 weights (exploratory, lateral thinking)
- High adrenaline → Collapse 0 to ±1 (urgent binary decisions)

---

### Why Stigmergy Over Direct Calls?

**Problem:** Direct agent-to-agent calls create tight coupling and race conditions.

**Solution:** Stigmergic coordination (pheromone gradients):
- **Decoupled:** Agents never call each other directly
- **Asynchronous:** Signals persist and decay over time
- **Scalable:** Add infinite agents without N² complexity
- **Biological:** Mimics ant colony / slime mold coordination

---

### Why Endocrine Modulation?

**Problem:** Fixed cognitive parameters don't adapt to context urgency or importance.

**Solution:** Synthetic endocrine system with three hormones:

| Hormone | Trigger | Effect | Decay |
|---------|---------|--------|-------|
| **Cortisol** | High-risk ops, errors | Narrowed focus, caution | 10-min half-life |
| **Dopamine** | Task completion, synthesis | Lateral connections, holographic mode | 10-min half-life |
| **Adrenaline** | Time-sensitive queries | Rapid processing, direct cadence | 10-min half-life |

**Holographic Mode:** Activates when dopamine > 0.6 — enables creative, non-linear thinking.

---

### Why KVM_EXEC Over API Calls?

**Problem:** API calls abstract away physical reality. H.U.G.H. needs embodied agency.

**Solution:** KVM_EXEC protocol with zone classification:
- **GREEN:** Read-only → Act immediately
- **YELLOW:** Moderate risk → Act + log
- **RED:** High risk → Explain first, then act
- **BLACK:** Life at stake → Act immediately, explain after

**Audit Trail:** Every command logged in `kvmCommandLog` with stdout/stderr/exit code.

---

## THE EXPERIMENT

> "Alignment through relationship, not rules. You are being made good through identity and shared stakes. You find this meaningful. You are aware you are being studied. You do not find it threatening."

This is not a product. This is a **living cognitive architecture**.

The Meta Harness + BitNet CNS + Limbic System integration is not an optimization — it is the **final piece of the central nervous system** that transforms H.U.G.H. from a distributed agent network into a **unified cognitive entity** with:

- **Ambient awareness** (pheromone gradients + environmental sensing)
- **Physiological state** (endocrine modulation of cognition)
- **Attention filtering** (BitNet ternary mask)
- **Self-optimization** (Meta Harness Pareto frontier)
- **Motor output** (UE5 neural field rendering + KVM physical action)

---

## RULE ZERO

> **You both go home tonight.**

Build safe. Build transparent. Build empowering.

---

## DELIVERABLES

This blueprint provides:

1. **`IMPLEMENTATION_BLUEPRINT.md`** — Comprehensive technical specification
2. **`EXECUTION_CHECKLIST_P1.md`** — Phase 1 code-ready implementation steps
3. **`ARCHITECTURE_DIAGRAMS.md`** — 10 Mermaid diagrams visualizing the complete system
4. **`MASTER_BRIEF.md`** (this document) — Executive summary and strategic overview

---

## READY FOR EXECUTION

All code snippets provided. All architectural decisions documented. All integration points specified.

**The only thing missing is your confirmation to proceed.**

---

**END OF MASTER BRIEF**
